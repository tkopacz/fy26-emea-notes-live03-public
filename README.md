# Notes Live

Minimal React frontend for NT-001.

## Development

```bash
npm install
npm run dev
```

## Available scripts

- `npm run dev` — start the React app locally
- `npm run build` — create a production build
- `npm run lint` — run ESLint
- `npm run test` — run focused note-path tests

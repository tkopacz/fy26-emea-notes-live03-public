<Window x:Class="NoteTaker.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NoteTaker.Wpf"
        xmlns:vm="clr-namespace:NoteTaker.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="Note Taker - WPF Client"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">

    <Window.Resources>
        <!-- Styles for consistent UI appearance -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Note Taker"
                       FontSize="24"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Margin="20,0,0,0"
                    VerticalAlignment="Center"/>
            <Button Content="Copy My URL"
                    Command="{Binding CopyUserUrlCommand}"
                    VerticalAlignment="Center"/>
        </StackPanel>

        <!-- User GUID Display -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Your Note Space ID:"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
            <TextBox Text="{Binding UserGuid, Mode=OneWay}"
                     IsReadOnly="True"
                     Width="300"
                     VerticalAlignment="Center"
                     BorderThickness="1"
                     Background="#F5F5F5"/>
        </StackPanel>

        <!-- Note Input Section -->
        <GroupBox Grid.Row="2" Header="Create New Note" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0"
                         Text="{Binding NoteContent, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         Height="100"
                         VerticalScrollBarVisibility="Auto"
                         ToolTip="Enter your note content here"/>

                <Button Grid.Row="1"
                        Content="Save Note"
                        Command="{Binding CreateNoteCommand}"
                        HorizontalAlignment="Right"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                        FontWeight="SemiBold"/>
            </Grid>
        </GroupBox>

        <!-- Notes List Section -->
        <GroupBox Grid.Row="3" Header="My Notes" Padding="10">
            <Grid>
                <!-- Loading Indicator -->
                <TextBlock Text="Loading notes..."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="Gray"
                           Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Notes List -->
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                    <ItemsControl ItemsSource="{Binding Notes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#DDDDDD"
                                        BorderThickness="1"
                                        CornerRadius="5"
                                        Padding="10"
                                        Margin="0,0,0,10"
                                        Background="White">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Note Content -->
                                        <TextBlock Grid.Row="0"
                                                   Text="{Binding Content}"
                                                   TextWrapping="Wrap"
                                                   FontSize="14"
                                                   Margin="0,0,0,5"/>

                                        <!-- Timestamp -->
                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding CreatedAt, StringFormat='Created: {0:g}'}"
                                                   FontSize="11"
                                                   Foreground="Gray"
                                                   Margin="0,0,0,5"/>

                                        <!-- Actions -->
                                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                                            <Button Content="Use as Basis"
                                                    Command="{Binding DataContext.UseAsBASISCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="5,2"
                                                    FontSize="11"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
